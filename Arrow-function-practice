const double = (arr) => arr.map((value) => value * 2);
const squareAndFindEvens = (numbers) =>
  numbers.map((value) => value ** 2).filter((value) => value % 2 === 0);

console.log(squareAndFindEvens([1, 2, 3, 4, 5]));
